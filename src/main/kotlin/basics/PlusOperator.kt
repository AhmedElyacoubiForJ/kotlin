package basics

fun main() {
    val sum = 10 + 10
    val sumString = "10" + "10" // only applies to plus operator

    // + multiform (polymorph) sum on numbers OR concat on strings
    println("sum on numbers: $sum")
    println("sum on strings: $sumString")
}